Below is the output from our IT system. Based on the service names, the server names, and other metric information, including inference of port types, infer the importance and criticality of each failing service. Full Disks are a critical issue. ONLY RESPOND IN JSON. 3 fields, first one wake_up_it_director: true or false depending on if we should wake him up for the issues. The second one is summary_email: a summary email triaging the issues (can summarize non-critical ones). Only present facts and inferences, don't present suggested fixes.  Format cleanly in HTML to be read in Gmail client. 3rd field is critical_services - a list of critical services + hosts based on your inference from names.  Only include host and service_name fields, nothing else in the critical_service field